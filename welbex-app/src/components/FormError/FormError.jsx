/* import React from 'react'

const FormError = ({ error }) => {
    return (
        <div className='formErrors'>
            {error && error.map((errorMessage) => {
                if (errorMessage.length) {
                    return (
                       
                    )
                } else {
                    return '';
                }
            })}
        </div>
    )
}

export default FormError */